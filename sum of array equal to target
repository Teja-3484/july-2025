def combinedsum(i,sub,ans,arr,target):
    if target<0:
        return
    if i==len(arr):
        return
    if target==0:
        ans.append(sub.copy())
        return
    sub.append(arr[i])
    combinedsum(i,sub,ans,arr,target-arr[i])
    sub.pop()
    combinedsum(i+1,sub,ans,arr,target-arr[i])
    return ans
arr=list(map(int,input().split()))
target=int(input())
i=0
sub=[]
ans=[]
print(combinedsum(i,sub,ans,arr,target))

